---
import { Image } from "astro:assets";
---

<section
  class="relative h-screen min-h-[750px] flex items-center overflow-hidden bg-[#020617]"
  style="min-height: 100vh; contain: layout;"
>
  <!-- Background Layer -->
  <div class="absolute inset-0 z-0">
    <img
      id="hero-bg"
      src="/images/tetikotomotiv-tuzla.webp"
      alt="Tetik Oto Servis"
      class="hero-bg-desktop hidden md:block w-full h-[120%] top-0 absolute object-cover object-center parallax-img will-change-transform"
      loading="eager"
      fetchpriority="high"
    />

    <!-- Mobile Background -->
    <img
      src="/images/tetikotomotiv-tuzla.webp"
      alt="Tetik Oto Servis"
      class="hero-bg-mobile md:hidden w-full h-full absolute inset-0 object-cover opacity-50"
      loading="eager"
      fetchpriority="high"
      width="720"
      height="1280"
    />

    <!-- Multi-layered Overlay for Vibrancy -->
    <!-- Gradient for Text Legibility (Right to Left) -->
    <div
      class="absolute inset-0 bg-gradient-to-l from-[#020617] via-[#020617]/70 to-transparent z-10"
    >
    </div>

    <!-- Left side subtle glow to car's tail lights -->
    <div
      class="absolute left-0 top-0 bottom-0 w-1/3 bg-gradient-to-r from-orange-600/10 to-transparent z-10 pointer-events-none"
    >
    </div>

    <!-- Scanline Effect (Movement) -->
    <div
      class="absolute inset-0 z-10 opacity-[0.03] pointer-events-none bg-[linear-gradient(rgba(18,16,16,0)_50%,rgba(0,0,0,0.25)_50%),linear-gradient(90deg,rgba(255,0,0,0.06),rgba(0,255,0,0.02),rgba(0,0,255,0.06))] bg-[length:100%_4px,3px_100%] animate-scanline"
    >
    </div>
  </div>

  <!-- Tech Grid -->
  <div
    class="absolute inset-0 z-0 opacity-[0.07] pointer-events-none"
    style="background-image: radial-gradient(circle, #f97316 0.5px, transparent 0.5px); background-size: 30px 30px;"
  >
  </div>

  <div class="container relative z-20 mx-auto px-4 md:px-8 flex justify-end">
    <div class="max-w-4xl text-right flex flex-col items-end">
      <!-- Top Badges -->
      <div class="flex flex-wrap gap-3 mb-10 animate-fade-right justify-end">
        <span
          class="px-5 py-2 bg-orange-600/10 border border-orange-500/30 backdrop-blur-xl rounded-full text-[11px] tracking-[0.2em] font-bold text-white flex items-center gap-3"
        >
          <span class="w-1.5 h-1.5 bg-orange-500 rounded-full animate-ping"
          ></span>
          TUZLA • PENDİK • GEBZE
        </span>
      </div>

      <!-- Main Slogan - Bold & Familiar -->
      <h1
        class="font-saira text-[clamp(2.5rem,12vw,6.5rem)] leading-[0.9] sm:leading-[0.85] font-bold text-white mb-6 sm:mb-8 tracking-tighter uppercase hero-title animate-text-pop"
      >
        <span class="block opacity-90 mb-1 sm:mb-2">JAPON & KORE</span>
        <span
          class="block text-transparent bg-clip-text bg-gradient-to-b from-orange-400 to-orange-600 animate-fade-in drop-shadow-[0_10px_20px_rgba(234,88,12,0.3)]"
        >
          BAKIM MERKEZİ
        </span>
      </h1>

      <!-- Status Integration -->
      <div
        class="flex items-center gap-6 mb-12 animate-fade-right delay-200 justify-end"
      >
        <div class="flex flex-col items-end">
          <span
            class="text-orange-500 font-saira font-black text-xl tracking-widest uppercase mb-1"
            >ÖZEL SERVİS</span
          >
          <div class="flex items-center gap-3">
            <span
              class="h-[1px] w-24 bg-gradient-to-l from-orange-500/50 to-transparent"
            ></span>
            <span
              class="text-white/40 text-[10px] uppercase font-bold tracking-tighter italic"
            >
              <span class="line-through decoration-orange-500/60">yetkili</span> değil
            </span>
          </div>
        </div>
      </div>

      <!-- Description -->
      <p
        class="text-base sm:text-lg md:text-2xl text-slate-300 mb-8 sm:mb-14 max-w-2xl leading-snug font-medium animate-fade-right delay-300 font-sans opacity-80"
      >
        Tetik Otomotiv: 17 yıllık uzmanlık ile Japon ve Kore araçlarınızda
        orijinal yedek parça güvencesi ve profesyonel tamir hizmeti.
      </p>

      <!-- Action Area -->
      <div
        class="flex flex-col sm:flex-row gap-8 animate-fade-right delay-400 justify-end w-full sm:w-auto"
      >
        <a
          href="/iletisim"
          class="group px-8 py-5 sm:px-12 sm:py-6 bg-white/5 hover:bg-white/10 text-white font-bold rounded-2xl border border-white/10 hover:border-orange-500/50 backdrop-blur-2xl transition-all duration-500 flex items-center justify-center gap-4 hover:-translate-y-2 order-2 sm:order-1"
        >
          <span class="uppercase tracking-widest text-xs sm:text-sm"
            >Randevu Al</span
          >
          <svg
            class="w-5 h-5 group-hover:translate-x-1 transition-transform"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
            ><path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2.5"
              d="M17 8l4 4m0 0l-4 4m4-4H3"></path></svg
          >
        </a>

        <a
          href="tel:05336157835"
          class="group relative px-8 py-5 sm:px-12 sm:py-6 bg-orange-600 text-white font-black rounded-2xl transition-all duration-500 shadow-[0_25px_50px_-12px_rgba(234,88,12,0.5)] hover:shadow-[0_0_60px_rgba(234,88,12,0.7)] hover:-translate-y-2 flex items-center justify-center gap-5 overflow-hidden order-1 sm:order-2"
        >
          <span
            class="relative z-10 uppercase tracking-[0.2em] sm:tracking-[0.25em] text-xs sm:text-sm"
            >Hemen Ara</span
          >
          <div
            class="w-10 h-10 bg-white/20 rounded-full flex items-center justify-center relative z-10 group-hover:scale-110 transition-transform"
          >
            <svg
              class="w-5 h-5 animate-bounce-slow"
              fill="currentColor"
              viewBox="0 0 24 24"
              ><path
                d="M6.62 10.79c1.44 2.83 3.76 5.15 6.59 6.59l2.2-2.2c.27-.27.67-.36 1.02-.24 1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.11.35.03.74-.25 1.02l-2.2 2.2z"
              ></path></svg
            >
          </div>
          <div
            class="absolute inset-0 -translate-x-full group-hover:translate-x-full transition-transform duration-1000 bg-gradient-to-r from-transparent via-white/40 to-transparent skew-x-12"
          >
          </div>
        </a>
      </div>
    </div>
  </div>

  <!-- Decorative Corner Elements -->
  <div
    class="absolute top-0 right-0 w-1/2 h-1/2 bg-gradient-to-bl from-orange-600/5 to-transparent pointer-events-none"
  >
  </div>
  <div
    class="absolute bottom-0 right-1/4 w-96 h-96 bg-blue-600/10 rounded-full blur-[150px] pointer-events-none animate-pulse-slow"
  >
  </div>
</section>

<style>
  .font-saira {
    font-family: "Saira", sans-serif;
  }

  @keyframes scanline {
    0% {
      background-position: 0 0;
    }
    100% {
      background-position: 0 100%;
    }
  }

  @keyframes gradient-slow {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }

  @keyframes fade-right {
    from {
      opacity: 0;
      transform: translateX(40px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes text-pop {
    0% {
      opacity: 0;
      transform: scale(0.95) translateY(20px);
    }
    100% {
      opacity: 1;
      transform: scale(1) translateY(0);
    }
  }

  @keyframes bounce-slow {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-3px);
    }
  }

  .animate-scanline {
    animation: scanline 10s linear infinite;
  }

  .animate-fade-right {
    opacity: 0;
    animation: fade-right 0.8s cubic-bezier(0.2, 1, 0.3, 1) forwards;
  }

  .animate-text-pop {
    opacity: 0;
    animation: text-pop 1s cubic-bezier(0.2, 1, 0.3, 1) forwards;
  }

  .animate-bounce-slow {
    animation: bounce-slow 2s ease-in-out infinite;
  }

  .animate-pulse-slow {
    animation: pulse 8s ease-in-out infinite;
  }

  .delay-200 {
    animation-delay: 0.2s;
  }
  .delay-300 {
    animation-delay: 0.3s;
  }
  .delay-400 {
    animation-delay: 0.4s;
  }

  .hero-bg-desktop {
    transition: transform 0.1s ease-out;
  }
</style>

<script>
  // Simplified Parallax
  const heroBg = document.getElementById("hero-bg");
  if (heroBg) {
    window.addEventListener(
      "scroll",
      () => {
        const scrollY = window.scrollY;
        if (scrollY < 1000) {
          const yPos = scrollY * 0.15;
          heroBg.style.transform = `translate3d(0, ${yPos}px, 0) scale(1.05)`;
        }
      },
      { passive: true },
    );
  }
</script>
